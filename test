好，我们就从 第 1 步，设计与准备 开始。
这一步的目标不是写代码，而是先把这次 POC 的范围、架构、成功标准、执行顺序定清楚。这样你后面在工作电脑上操作时，不会边做边改方向。

我先给你一个全局图，然后给你这一步该产出的内容，再给你几个可以丢给工作电脑 AI 的简短提示词。


---

先看全局

这次事情的主线可以一直记成这 6 步：

1. 先把 POC 设计讲清楚


2. 用 Terraform 起最小 EKS 环境


3. 部署一个简单测试应用


4. 梳理 FIS pod action 需要的权限链路


5. 跑最小实验验证


6. 记录结论，销毁资源



你现在在做的是 第 1 步。
这一步做得好，后面会很顺。


---

第 1 步要达成什么

今天这一步，你只需要把下面这些东西定下来：

已经定下来的

Region 用 eu-west-1

用 Terraform

起一个最小 EKS cluster

用 2 个 worker nodes

节点规格是 t3.medium

两个节点分布在 两个不同 AZ

实验做完就删


我建议现在也一起定下来的

先用 EKS Managed Node Group

先用 public subnets

先不做 NAT Gateway

先不接 Istio、ALB、Ingress 这些复杂东西

先部署一个简单测试应用

第一轮实验先做最简单的 fault，不一开始碰最复杂的 network case



---

为什么我建议 public subnets

因为你这次的目的不是做生产网络设计，而是验证 FIS pod action 的前置条件和行为。

先用 public subnets 的好处是：

更便宜

更简单

少一个 NAT Gateway 成本和变量

更适合短期 sandbox 实验


你这次最怕的不是“不够像生产”，而是“还没开始验证 FIS，就先被网络复杂度拖住”。


---

第 1 步的设计结论

下面这段，就是你这次 POC 当前应该采用的设计基线。你可以直接当作你的设计说明。

POC 目标

在 eu-west-1 使用 Terraform 创建一个最小 EKS sandbox 环境，包含两个不同 AZ 的 t3.medium worker nodes，用于验证 AWS FIS EKS pod action 的前置条件、权限要求和实际行为。

POC 范围

本次只关注最小可验证路径：

创建 VPC 和 EKS cluster

创建一个 managed node group

2 个节点分布在 2 个 AZ

部署一个简单测试应用

准备并验证 FIS pod action 所需的基础权限和对象

完成最小实验后销毁所有资源


本次先不做

不做 NAT Gateway

不做 private nodes

不做 Istio / service mesh 集成

不做复杂 observability

不做生产级高可用和安全加固

不一开始做复杂 network disruption 方案


架构原则

region 为 eu-west-1

AZ 不手写死，实际以账户里可用的前两个 AZ 为准

使用 public subnets

使用 EKS Managed Node Group

节点规格为 t3.medium

节点数固定为 2

测试完成后通过 Terraform destroy 清理资源


成功标准

这次 POC 成功，不是指“一定要把所有 chaos action 都跑通”，而是至少做到下面几件事：

1. EKS 集群可以成功创建


2. 两个节点都能正常 Ready


3. 可以成功部署一个测试应用


4. 能开始梳理 FIS pod action 所需的 IAM 和 Kubernetes 权限链路


5. 至少完成一次最小实验验证，或者明确知道失败点在哪里




---

这一步你真正要产出的 4 个东西

今天建议只产出这 4 个内容。

1. 一个 POC 设计说明

很短就行，控制在一页内。
内容包括：

目标

范围

架构选择

不做什么

成功标准


2. 资源清单

你要明确这次准备创建什么资源。

建议清单是：

1 个 VPC

2 个 public subnets

1 个 EKS cluster

1 个 managed node group

2 个 t3.medium EC2 nodes

基础安全组和 IAM 配置

1 个测试 namespace

1 个简单 deployment


3. 命名方案

你最好现在就统一一下命名，后面会省很多事。

比如：

cluster name
fis-poc-eks-euw1

node group name
main-ng

VPC name
fis-poc-vpc

demo namespace
fis-demo


名字不用完美，但要统一。

4. 执行顺序

也就是你接下来会按什么顺序推进。

建议顺序就是：

1. 定设计


2. 起 Terraform 基础环境


3. 验证 kubectl


4. 部署 demo app


5. 补 FIS 相关权限和对象


6. 做实验


7. destroy




---

你在工作电脑上今天应该做什么

你不需要今天就把所有 Terraform 写完。
你今天的任务更适合是：

今天的操作目标

1. 准备一个本地项目目录


2. 写出 POC 设计说明


3. 确定命名


4. 确定 Terraform 目录结构


5. 确定后续 apply 的资源边界



今天完成的标志

当你完成今天这一步时，你应该能清楚说出：

我要建哪些资源

为什么这样建

为什么暂时不建 NAT

为什么先用 public subnet

为什么先从最小实验开始

后面每一步要怎么推进



---

你现在可以直接采用的目录结构

这一步先规划结构，不一定立刻把文件都写满。

eks-fis-poc/
├── README.md
├── providers.tf
├── versions.tf
├── variables.tf
├── terraform.tfvars
├── main.tf
├── outputs.tf
├── vpc.tf
├── eks.tf
├── iam.tf
├── locals.tf
└── manifests/
    ├── namespace.yaml
    └── demo-app.yaml

这已经够做第一版 POC 了。
不要一开始拆太多 module，不然会把精力耗在结构洁癖上。


---

关于 AZ 的处理

这里提醒你一个细节。
不要自己先拍脑袋写死 eu-west-1a 和 eu-west-1b。

因为 AWS 的 AZ 字母映射是按账户视角来的。
最稳的方式是：

在你的账户里查当前可用 AZ

选前两个可用的

Terraform 里让它动态取，或者你在 tfvars 里明确填


所以这一步的原则是：

地区固定为 eu-west-1，具体 AZ 名称以账户实际可用为准。


---

Kubernetes version 现在怎么定

我建议你现在先不要写死具体版本号，避免后面版本支持状态变了。

这一步只定原则：

使用一个 EKS 当前仍在 standard support 的版本

不使用 extended support 版本

后面真正写 Terraform 时，再在控制台或文档里确认当下可用版本


这样你现在不会被版本号卡住。


---

我建议你现在保存的“项目原则”

你可以把这几条当作这次 POC 的总原则：

1. 先打通最小闭环，不追求完美


2. 先排除无关复杂度，尤其是 NAT 和复杂网络


3. 先让 EKS 能稳定创建并可操作


4. 先放一个简单应用，给 FIS 一个明确目标


5. 先验证最简单的 action，再逐步增加复杂度


6. 所有资源必须能快速 destroy



这 6 条其实就是你后面所有操作的判断标准。


---

给你一个可以发给工作电脑 AI 的简短提示词

你说你可以让工作电脑上的 AI 产出代码或文档，那我给你几个短提示词。
先从文档开始，不急着写 Terraform 代码。

提示词 1，用来生成 POC 设计说明

Help me write a short design note for a sandbox POC.

Goal:
Use Terraform to create a minimal Amazon EKS cluster in eu-west-1 for AWS FIS EKS pod action testing.

Requirements:
- two worker nodes
- t3.medium
- nodes in two different AZs
- use public subnets
- no NAT Gateway for now
- use EKS managed node group
- deploy a simple demo app later
- keep the environment minimal and cheap
- destroy everything after testing

Please produce a concise one-page design note with:
1. objective
2. scope
3. architecture decisions
4. what is intentionally out of scope
5. success criteria

提示词 2，用来生成 Terraform 项目骨架

Create a minimal Terraform project structure for an EKS sandbox POC in eu-west-1.

Requirements:
- separate files for providers, variables, locals, vpc, eks, iam, outputs
- keep the structure simple
- no modules created by us yet
- include a README outline
- this is only the project skeleton, not full implementation yet

提示词 3，用来生成 README 初稿

Write a concise README for a Terraform-based EKS sandbox POC project.

Context:
- region eu-west-1
- minimal EKS cluster
- 2 t3.medium worker nodes in 2 AZs
- public subnets
- no NAT Gateway initially
- purpose is AWS FIS EKS pod action testing
- environment should be easy to destroy after testing

Please include:
- purpose
- architecture summary
- deployment plan
- cleanup reminder


---

我建议你现在就这样推进

你今天先做这 3 件事就够了：

1. 在工作电脑上建一个项目目录


2. 用上面的提示词生成一个简短设计说明和 README


3. 把命名和资源边界定下来



做完这一步后，我们再进入 第 2 步，也就是：

把 Terraform 第一版骨架搭出来，只先建 VPC + EKS + managed node group。


---

这一步的简要总结

你现在第 1 步已经有了明确方向：

region 用 eu-west-1

工具用 Terraform

架构先走最小化

2 个 t3.medium 节点，分布在两个 AZ

先 public subnet

先不做 NAT

目标是验证 FIS pod action，而不是搭生产平台


今天的产出应该是：

一页 POC 设计说明

一个项目目录结构

一个 README 初稿

统一的命名方案

明确的下一步执行顺序


等你在工作电脑上把这几个东西准备好，我们就继续第 2 步。
